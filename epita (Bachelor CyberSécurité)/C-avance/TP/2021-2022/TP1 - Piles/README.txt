The hard tests with the static version might not work, because of the way values are stored on the CPU stack.

Les tests "hard" de la version "static" peuvent ne pas fonctionner à cause de la façon dont les valeurs sont stockées sur la pile du CPU.

